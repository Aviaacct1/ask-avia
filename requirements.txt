# ask-avia runtime dependencies.
#
# Install ONLY what this host runs. Meridian's requirements.txt listed streamlit for a
# legacy tool the workstation does not run, and pip resolved starlette DOWN from 1.6.0
# to 1.3.1 to satisfy it; starlette is what the live portal server runs on. A tool
# nobody was using changed a dependency of the tool in use, in a resolver step nobody
# reads. Anything legacy goes in its own file, never here.
#
# One virtual environment for this tool. The workstation runs four.

duckdb>=1.1,<2
openpyxl>=3.1,<4

# Major pinned deliberately. On 8 August 2026 a bare `mcp>=1.2` resolved to 2.0.0 on the
# Dev PC, which is a different API from 1.x. Unpinned, a later host could resolve either
# and the server would meet an interface it was not written against, in a resolver step
# nobody reads. The service targets the 2.x API.
mcp>=2.0,<3

httpx>=0.27,<1

# Resolved on the Dev PC, 8 August 2026, Python 3.12.10, this tool's own venv:
#   duckdb 1.5.5, openpyxl 3.1.5, mcp 2.0.0 (mcp-types 2.0.0), httpx 0.28.1,
#   starlette 1.6.0, uvicorn 0.52.1, pydantic 2.13.4.
# starlette is recorded because a legacy set on a SHARED interpreter pulled it down from
# 1.6.0 to 1.3.1 on the same day, underneath the live portal server. Isolated here.
